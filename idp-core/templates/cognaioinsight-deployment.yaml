apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.cognaioinsight.name }}
  labels:
    {{- include "cognaio.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      name: {{ .Values.cognaioinsight.name }}
      {{- include "cognaio.selectorLabels" . | nindent 6 }}
  replicas: {{ .Values.cognaioinsight.replicaCount }}
  template:
    metadata:
      labels:
        name: {{ .Values.cognaioinsight.name }}
        appgroup: cognaio-idp
        {{- include "cognaio.selectorLabels" . | nindent 8 }}
    spec:
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      {{- if .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml .Values.imagePullSecrets | nindent 6 }}
      {{- end }}
      containers:
      - name: {{ .Values.cognaioinsight.name }}
        image: {{ .Values.image.containerRegistry }}/{{ .Values.cognaioinsight.image.name }}:{{ .Values.cognaioinsight.image.tag }}
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        securityContext:
           {{- toYaml .Values.securityContext | nindent 10 }}
        ports:
        - containerPort: 8080
        env:
        - name: NODE_ENV
          value: {{ .Values.cognaioinsight.env.node_env }} 
        volumeMounts:
        - mountPath: /usr/share/nginx/html/assets/configs.json
          subPath: configs.json
          name: config
        - mountPath: /etc/nginx/nginx.conf
          subPath: nginx.conf
          name: config
        {{- if .Values.cognaioinsight.resources }}
        resources: {{- toYaml .Values.cognaioinsight.resources | nindent 12 }}
        {{- end }}
      volumes:
      - configMap:
          name: cognaioinsight-configs
        name: config