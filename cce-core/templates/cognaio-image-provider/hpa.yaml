{{- if .Values.imageprovider.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.imageprovider.name }}
  labels:
    {{- include "cognaio.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.imageprovider.name }}
  minReplicas: {{ .Values.imageprovider.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.imageprovider.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.imageprovider.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.imageprovider.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.imageprovider.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.imageprovider.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
