{{- if .Values.cognaioinsight.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.cognaioinsight.serviceAccount.name }}
  labels:
    {{- include "cognaio.labels" . | nindent 4 }}
  {{- with .Values.cognaioinsight.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: {{ .Values.cognaioinsight.serviceAccount.automount }}
{{- end }}
