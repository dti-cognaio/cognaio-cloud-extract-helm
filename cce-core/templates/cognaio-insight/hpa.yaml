{{- if .Values.cognaioinsight.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.cognaioinsight.name }}
  labels:
    {{- include "cognaio.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.cognaioinsight.name }}
  minReplicas: {{ .Values.cognaioinsight.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.cognaioinsight.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.cognaioinsight.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.cognaioinsight.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.cognaioinsight.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.cognaioinsight.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
