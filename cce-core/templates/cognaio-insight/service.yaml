apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.cognaioinsight.service.name }}
  labels:
    {{- include "cognaio.labels" . | nindent 4 }}
    app: cognaio-idp
    name: {{ .Values.cognaioinsight.service.name }}
spec:
  ports:
  - name: http
    port: {{ .Values.cognaioinsight.service.port }}
    protocol: TCP
    targetPort: {{ .Values.cognaioinsight.env.port }}
  selector:
    {{- include "cognaio.selectorLabels" . | nindent 4 }}
    {{- with .Values.cognaioinsight.podSelectorLabels }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
  type: {{ .Values.cognaioinsight.service.type }}