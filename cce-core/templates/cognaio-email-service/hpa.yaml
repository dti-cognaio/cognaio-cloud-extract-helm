{{- if .Values.emailservice.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.emailservice.name }}
  labels:
    {{- include "cognaio.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.emailservice.name }}
  minReplicas: {{ .Values.emailservice.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.emailservice.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.emailservice.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.emailservice.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.emailservice.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.emailservice.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
